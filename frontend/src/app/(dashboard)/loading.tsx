// ─────────────────────────────────────────────────────────────────────────────
// src/app/(dashboard)/loading.tsx  — dashboard-level loading
import { PageSkeleton } from '@/components/common/loading-skeleton'
export default function DashboardLoading() {
  return <PageSkeleton />
}
